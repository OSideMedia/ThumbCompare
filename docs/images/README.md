# Screenshot Assets

Add screenshot files here so they render in the main project README.

Expected filenames:
- `compare-view.png`
- `export-example.png`

You can replace them with your own captures at any time.
